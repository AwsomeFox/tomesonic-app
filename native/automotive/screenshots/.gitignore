# Where capture.sh writes during a run: PNGs, uiautomator dumps, logcat. The
# workflow copies the PNGs into shots/ (which IS committed) and uploads the rest
# as artifacts — none of it belongs in the repo from a local run.
out/

# Optional audio fixture (see README § No audio fixture). Not generated by this
# rig's workflow; a fixture, never source.
silence.mp3

# Byte-compiled mock server (`python3 -m py_compile mock_abs.py`).
__pycache__/
