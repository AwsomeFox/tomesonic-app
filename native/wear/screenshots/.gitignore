# Generated by the workflow's ffmpeg step (600s of silence, ~2.4 MB). Every
# track of every mock item streams from it; it is a fixture, not source.
silence.mp3

# Byte-compiled mock server (`python3 -m py_compile mock_abs.py`).
__pycache__/
