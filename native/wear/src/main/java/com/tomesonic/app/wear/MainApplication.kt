package com.tomesonic.app.wear

import android.app.Application

// Wave 2 initialises the Graph service locator here; Wave 3A adds the playback
// notification channel. Empty until then so the scaffold compiles on its own.
class MainApplication : Application()
